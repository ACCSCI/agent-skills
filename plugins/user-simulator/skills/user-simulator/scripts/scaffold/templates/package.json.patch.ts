{
  "_comment": "Patcher for package.json — merges into existing file. See scripts/scaffold/scaffold.ts for the runtime invocation.",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
